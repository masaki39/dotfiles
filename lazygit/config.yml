# https://github.com/jesseduffield/lazygit/blob/master/docs/Config.md

customCommands:
  - command: czg ai
    context: files
    key: c
    output: terminal
  - command: czg
    context: files
    key: C
    output: terminal
gui:
  language: "en"
  showIcons: true
# log customize
git:
  branchLogCmd: "git log --graph --color=always --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' {{branchName}} --"
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never # ここの設定を使用する場合はdeltaのインストールが必要
